<% provide(:title, "New Group")  %>
<div class="jumbotron">
<h3 class="center">Add Group</h3>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
	<%= form_for(@group) do |f| %>
	  <%= render 'shared/error_messages', object: f.object %>

	  <%= f.label :id %>
	  <%= f.text_field :groupid64, class: 'form-control' %>

	  <%= f.label :name %>
	  <%= f.text_field :name, class: 'form-control' %>

	  <%= f.label :url %>
	  <%= f.text_field :url, class: 'form-control' %>

	  <%= f.submit "Save changes", class: "btn btn-50 btn-primary pull-left" %>
	  <%= link_to "Back", groups_url, class: "btn btn-50 btn-primary pull-right" %>
	<% end %>
  </div>
</div>
</div>