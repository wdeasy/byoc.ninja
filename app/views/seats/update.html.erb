<% provide(:title, "Update Seats")  %>
<div class="jumbotron">
<h3 class="center">Update Seats</h3>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
	<%= form_tag(seats_update_path, method: "get") do %>
	  <%= hidden_field :update, "true" %>
	  <%= submit_tag("Update", class: "btn btn-50 btn-primary pull-left") %>
	  <%= link_to "Back", protocols_url, class: "btn btn-50 btn-primary pull-right" %>
	<% end %>
  </div>
</div> 
</div>